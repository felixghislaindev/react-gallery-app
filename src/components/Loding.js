// this component will be used to display a loading message while data
// is being pulled/ request during the api call
import React from "react";

const Loading = () => {
  return <h1>Data loding..</h1>;
};

export default Loading;
